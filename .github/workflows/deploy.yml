name: Fly Deploy

on:
  push:
    branches: [main]

env:
  FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

jobs:
  # Deploy to Fly.io after all CI tests pass
  deploy:
    name: Deploy app
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v3
      - uses: superfly/flyctl-actions/setup-flyctl@v2
      - run: flyctl deploy --remote-only
